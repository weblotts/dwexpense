/**
 * Buckets are now seeded per user automatically on registration
 * (see seedDefaultBuckets in db.ts). This standalone script is kept only
 * as an informational no-op.
 */
console.log(
  'Nothing to seed: starter categories are created automatically when a user registers.'
);
